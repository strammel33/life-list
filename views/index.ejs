<%- include('./partials/html-head') %>
<%- include('./partials/nav') %>

<main>
  <div class="left">
    <h3>big bird picture</h3>
  </div>
  <div class="right">
    <% if (user) { %>
      <button>My Records</button>
      <button>My Life List</button>
      <button>Add Record</button>
    <% } else { %>
      <h2>LIFE LIST (noun):</h2>
      <p>a record of all birds sighted and identified by a birder</p>
    <% } %>
  </div>
</main>
<section class="space">

</section>

<%- include('./partials/footer') %>
